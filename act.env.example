# act 実行時に読み込む環境変数の例
# 必要に応じて値を設定し、act.env としてコピーして利用する

# Playwright のベース URL（E2E テスト用）
PLAYWRIGHT_BASE_URL=http://localhost:3001

# フロントエンド API エンドポイント（バックエンド API ポートを指定）
NEXT_PUBLIC_API_URL=http://localhost:8001

# OAuth トークン暗号鍵（テスト専用。例: `python - <<'PY' ...` で生成した値を設定）
# OAUTH_TOKEN_ENCRYPTION_KEY=<urlsafe_base64_32byte_key>
