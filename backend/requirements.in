# 直接依存のみ（パッチレベルまで固定）
# 依存関係の追加・更新はこのファイルに対してのみ実施すること。

fastapi==0.115.6
uvicorn[standard]==0.27.1
pydantic==2.10.4
pydantic-settings==2.6.1
email-validator==2.1.1
python-dotenv==1.0.1
docker==7.0.0
# Docker ソケット回帰と既知の CVE 回避のために requests を厳密ピンニング
requests==2.31.0
httpx==0.27.0
cryptography==44.0.1

# Test dependencies
pytest==8.1.1
pytest-asyncio==0.23.5
hypothesis==6.98.15
